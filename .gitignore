# Environment variables — never commit real secrets
.env
.env.local
.env.*.local

# Netlify
.netlify/

# Dependencies (if any tooling is added later)
node_modules/

# OS / editor files
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Held-for-review images (not part of the published site)
images/_held-for-review/
